appId: twinkler
---
# Resume the existing process: the native player must still report active playback.
- launchApp:
    stopApp: false
- assertVisible:
    id: "music-playing"
- assertVisible:
    id: "session-music-on"
- takeScreenshot: BG-MUSIC-resumed-playing
