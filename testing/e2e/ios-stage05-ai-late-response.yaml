appId: twinkler
# Фрагмент без тегов: не подхватывается планировщиком тиров. По замыслу
# исполняется runFlow внутри сессии с задержанным ИИ-ответом (stub :9085);
# standalone запуск бессмысленен — сессии нет. См. отчёт 2026-09-20.
---
- assertVisible:
    id: "dock-answer-button"
