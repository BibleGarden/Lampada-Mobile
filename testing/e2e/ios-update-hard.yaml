appId: twinkler
# NEEDS-STUB: stub /api/version-check с update_type=hard (run-stub-phase.sh).
# В теговые прогоны не входит.
---
# Жёсткое обновление: баннер без «позже», переживает перезапуск.
- launchApp:
    stopApp: true
- extendedWaitUntil:
    visible:
      id: update-hard
    timeout: 20000
- assertVisible:
    id: "update-hard"
- assertNotVisible:
    id: update-later
- takeScreenshot: UPDATE-hard-banner
# Смена экрана не снимает жёсткий баннер.
- openLink: "lampada://settings"
- assertVisible:
    id: update-hard
- openLink: "lampada://"
- assertVisible:
    id: update-hard
# Холодный старт: баннер снова.
- launchApp:
    stopApp: true
- extendedWaitUntil:
    visible:
      id: update-hard
    timeout: 20000
- takeScreenshot: UPDATE-hard-relaunch
