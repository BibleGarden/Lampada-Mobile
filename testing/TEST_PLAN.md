# Тест-план Twinkler

- Назначение: версионируемый каталог сценариев; статусы выполнения ведутся в ClickUp
- Продукт: Expo SDK 57 / React Native, iOS и Android
- Основная платформа первого прогона: iOS, собственная Release-сборка

## Источники правды

- [корневая задача ClickUp](https://app.clickup.com/t/86cb8k50b) — этапы,
  исполнители, статусы выполнения и дефекты;
- этот тест-план — стабильные ID сценариев и ожидаемые результаты;
- `reports/` — датированные результаты конкретных прогонов;
- `evidence/` — отобранные подтверждения, на которые ссылаются отчёты.

Текущие статусы сценариев в этом файле не хранятся.

## Этапы тестирования

| № | Этап |
|---:|---|
| 01 | Smoke |
| 02 | Технические риски |
| 03 | Навигация, настройка и таймер |
| 04 | Ответы и аудио |
| 05 | ИИ, сеть и приватность |
| 06 | Писание, завершение и дневник |
| 07 | Устройства и доступность |
| 08 | Надёжность и производительность |
| 09 | Автоматизация |
| 10 | Регрессия и готовность к релизу |
| 99 | Отложенные и принятые дефекты |

## 1. Цель

Проверить, что пользователь может пройти полный цикл молитвы без потери данных:

1. открыть приложение;
2. выбрать цель и длительность;
3. начать молитву удержанием кнопки;
4. отвечать текстом и голосом, читать и сохранять места Писания;
5. завершить молитву и сохранить вывод;
6. найти результат в дневнике после перезапуска приложения.

Дополнительно проверить устойчивость при отказе микрофона, сети и ИИ, корректность
локального хранения и отсутствие неожиданной отправки пользовательских данных.

## 2. Вне объёма первого прогона

- качество богословских формулировок ИИ оценивается отдельно человеком;
- нагрузочное тестирование серверного AI-прокси;
- публикация и обновление через App Store / Google Play;
- исчерпывающая проверка всех версий iOS и Android.

## 3. Главные риски

| Приоритет | Риск | Что проверяем |
|---|---|---|
| P0 | Потеря ответа или записи | сохранение при закрытии шторки, окончании таймера и перезапуске |
| P0 | Падение или тупик в основном потоке | переходы `setup → threshold → session → reflect → done` |
| P0 | Нарушение приватности | настройку передачи ответов, тело AI-запроса, локальность аудио |
| P1 | Ошибки нативных модулей | Skia, Reanimated, Gesture Handler, SQLite и Expo Audio в собственном билде |
| P1 | Ошибки таймера | фон/передний план, изменение времени, авто- и досрочное завершение |
| P1 | Повреждение дневника | поиск, детали, воспроизведение и каскадное удаление |
| P1 | Недоступность ИИ | отсутствие конфигурации, тайм-аут, HTTP-ошибка, пустой ответ |
| P2 | Ошибки календаря | серия дней, повторное завершение в один день, граница суток и часовой пояс |
| P2 | Проблемы интерфейса | маленький экран, iPad, клавиатура, safe area, длинные тексты |

## 4. Окружения

### Обязательные для первого прогона

| Окружение | Назначение |
|---|---|
| Собственный iOS Release-билд на физическом iPhone | основной пользовательский прогон, микрофон, звук, хаптика, сохранение после перезапуска |
| iOS Simulator | быстрые повторные проверки экранов, навигации и размеров |
| Проверки исходного кода | TypeScript и диагностика совместимости Expo SDK |

### Следующий этап

| Окружение | Назначение |
|---|---|
| Android Emulator и физическое устройство | системная кнопка «назад», разрешения, запись и воспроизведение |
| iPad / iPad Simulator | `supportsTablet`, portrait layout, ширина и масштабирование |
| Web | только smoke-проверка, если web является поддерживаемой платформой продукта |

Для каждого отчёта фиксируются модель устройства, версия ОС, commit, тип сборки,
наличие AI-конфигурации и состояние базы до начала прогона.

## 5. Наборы тестовых данных

- чистая установка без записей и настроек;
- установка с одной завершённой молитвой;
- история из 20+ молитв с похожими русскоязычными целями;
- пустая, короткая и очень длинная цель, кириллица, emoji и переносы;
- молитва без таймера, на 5 минут и с изменённым в процессе таймером;
- ответы: пустой, только пробелы, длинный текст, несколько аудиозаписей;
- AI выключен, работает, отвечает медленно и возвращает ошибку;
- разрешение микрофона выдано, отклонено и ранее запрещено в настройках ОС.

## 6. Проверки перед запуском приложения

| ID | Проверка | Ожидаемый результат |
|---|---|---|
| PRE-001 | Установить зависимости по lock-файлу | установка завершается без ошибки |
| PRE-002 | Выполнить `npm run typecheck` | exit code 0, полный лог сохранён в отчёте |
| PRE-003 | Выполнить `npx expo-doctor` | нет необъяснённых несовместимостей SDK 57 |
| PRE-004 | Собрать приложение штатным способом | Release-билд устанавливается и запускается |

Expo Go не использовать: он не содержит часть нативных модулей проекта.

## 7. Smoke-сценарий

Smoke считается пройденным только целиком.

| ID | Действия | Ожидаемый результат |
|---|---|---|
| SMK-001 | Холодный запуск | главный экран появляется без зависания и ошибок |
| SMK-002 | Открыть настройку молитвы, указать цель и 5 минут | значения отображаются и переходят на экран порога |
| SMK-003 | Отпустить удержание раньше времени, затем удержать полностью | первый жест отменяется, второй начинает ровно одну сессию |
| SMK-004 | Открыть ответ, сохранить текст | ответ отмечается сохранённым и не теряется |
| SMK-005 | Завершить досрочно, сохранить вывод | открывается экран успешного завершения |
| SMK-006 | Вернуться на главную и открыть дневник | молитва, ответ и вывод присутствуют |
| SMK-007 | Принудительно закрыть и снова открыть приложение | сохранённые данные и серия дней остаются |

## 8. Функциональные сценарии

### Главный экран и навигация

| ID | Сценарий | Ожидаемый результат |
|---|---|---|
| NAV-001 | Первый запуск с чистой базой | корректное приветствие, пустая неделя, доступные дневник/настройки/старт |
| NAV-002 | Возврат на главный экран после незавершённой настройки | черновая сессия сбрасывается, приложение не зависает |
| NAV-003 | Открытие экранов по deep link без истории переходов | кнопка назад ведёт на главный экран или безопасно закрывает экран |
| NAV-004 | Android Back во время молитвы и рефлексии | системный жест не обрывает обязательный поток |
| NAV-005 | Быстрые многократные нажатия на переходы | не создаются дубли экранов и сессий |

### Настройка и вход в молитву

| ID | Сценарий | Ожидаемый результат |
|---|---|---|
| SETUP-001 | Оставить цель пустой | доступна свободная молитва, тексты не содержат пустых/сломанных фраз |
| SETUP-002 | Выбрать каждый пример цели | модальное окно закрывается, выбранный текст появляется в поле |
| SETUP-003 | Проверить пресеты 5/15/30/60/∞ и кнопки ± | значение и склонение минут корректны, границы безопасны |
| SETUP-004 | Длинная цель и открытая клавиатура | поле остаётся управляемым, кнопка «Далее» доступна после закрытия клавиатуры |
| START-001 | Короткое удержание и уход пальца за пределы | прогресс сбрасывается, сессия не создаётся |
| START-002 | Полное удержание | создаётся одна сессия и открывается таймер |
| START-003 | Повторные жесты во время перехода | не создаются параллельные сессии |
| START-004 | Ошибка SQLite при создании сессии | кнопка не остаётся навсегда заблокированной, ошибка диагностируема |

### Таймер и поток молитвы

| ID | Сценарий | Ожидаемый результат |
|---|---|---|
| SES-001 | Конечный таймер | каждую секунду уменьшается, на нуле один раз открывает рефлексию |
| SES-002 | Режим ∞ | отображается прошедшее время, автоматического завершения нет |
| SES-003 | Изменить таймер кнопками −/+ | время меняется на ожидаемый шаг и не становится некорректным |
| SES-004 | Свернуть приложение и вернуть через 10–60 секунд | поведение таймера соответствует выбранной продуктовой политике; расхождение фиксируется |
| SES-005 | Досрочно завершить | открытый ответ сохраняется, затем открывается рефлексия один раз |
| SES-006 | На рефлексии выбрать «Вернуться к молитве» | начинается новый отсчёт с той же целью без потери уже сохранённых ответов |
| SES-007 | Очень длинная цель | текст не перекрывает таймер и панель спутника |

### Музыкальное сопровождение

| ID | Сценарий | Ожидаемый результат |
|---|---|---|
| MUS-001 | Включить и выключить музыку кнопкой в сессии | кнопка и индикатор меняют состояние, воспроизведение запускается и останавливается |
| MUS-002 | Запустить приложение без сети и включить музыку | встроенные композиции доступны полностью офлайн |
| MUS-003 | Дождаться конца плейлиста | пятнадцать треков последовательно воспроизводятся и цикл начинается заново |
| MUS-004 | Свернуть приложение с включённой музыкой и вернуться | в фоне музыка приостанавливается, после возврата возобновляется при сохранённом включённом состоянии |
| MUS-005 | При включённой музыке записать голос и прослушать черновик | музыка останавливается до начала записи/прослушивания и возобновляется после, в голосовой записи её нет |
| MUS-006 | Завершить молитву досрочно или по таймеру | музыкальный плеер останавливается и не играет на экранах рефлексии, завершения и следующей сессии |
| MUS-007 | Довести таймер до нуля при включённой музыке | переход на рефлексию не вызывает crash `ERR_NATIVE_SHARED_OBJECT_NOT_FOUND` после удаления плейлиста |
| MUS-008 | Начать несколько молитв подряд | стартовый трек выбирается случайно и не повторяет старт предыдущей сессии в текущем запуске приложения |

### Ответы и аудио

| ID | Сценарий | Ожидаемый результат |
|---|---|---|
| ANS-001 | Сохранить текст, вновь открыть тот же вопрос | сохранённый текст восстановлен |
| ANS-002 | Закрыть пустую шторку | закрывается без лишнего подтверждения |
| ANS-003 | Закрыть несохранённый непустой ответ | требуется явное повторное подтверждение, данные не теряются молча |
| ANS-004 | Отказать в доступе к микрофону | приложение не падает и остаётся управляемым |
| ANS-005 | Записать и остановить одно аудио | появляется запись с ненулевой длительностью, она воспроизводится |
| ANS-006 | Создать несколько записей | файлы различны, предыдущие записи не перезаписаны |
| ANS-007 | Сохранить во время активной записи | запись корректно останавливается и сохраняется |
| ANS-008 | Удалить запись с подтверждением | запись исчезает из UI, БД и файлов после сохранения |
| ANS-009 | Попытаться закрыть шторку записей свайпом или по фону во время записи | шторка не закрывается; микрофон остаётся под видимым управлением до «Готово»; после остановки шторка закрывается обычным способом |
| ANS-010 | Таймер заканчивается при открытой шторке | таймер остаётся на `0:00` с подсказкой «закончи ответ»; текст и запись не обрываются; переход на рефлексию происходит один раз после явного сохранения или закрытия шторки |
| ANS-011 | Переключаться между вопросами и редактировать старый ответ | ответ сохраняется под правильным вопросом |
| ANS-012 | Остановить запись и не нажимать «Расшифровать» | появляется аудиозапись и кнопка; сетевой запрос не выполняется, токены не расходуются |
| ANS-013 | Нажать «Расшифровать», затем сохранить во время запроса | появляется состояние загрузки; сохранение дожидается запроса, аудио и текст восстанавливаются после повторного открытия |
| ANS-014 | Получить offline, timeout или HTTP-ошибку расшифровки | аудио остаётся доступным, отображается «Повторить», повторная попытка может завершиться успешно |
| ANS-015 | Удалить или отменить запись во время расшифровки | запрос отменяется, поздний ответ не возвращает удалённую запись в UI или БД |
| ANS-016 | Открыть длинную расшифровку в шторке записей | блок показывает три строки, «Показать полностью» раскрывает и сворачивает его; список записей остаётся прокручиваемым |
| ANS-017 | Нажать «Добавить в ответ» на пустом и на заполненном ответе | шторка записей закрывается, текст расшифровки дописывается в конец поля ответа через пустую строку, сама расшифровка не меняется |
| ANS-018 | Убрать расшифровку крестиком и сохранить | карточка возвращается к одной строке с кнопкой «Расшифровать», запись остаётся, перенесённый в ответ текст не исчезает |
| ANS-019 | На пустом ответе быстро повторно нажать микрофон/«Записать» во время запуска | выполняется один запуск recorder, кнопки и закрытие недоступны до завершения pending-состояния, скрытой записи нет |
| ANS-020 | Быстро повторно нажать «Готово» при остановке записи | выполняется один stop, появляется одна рабочая запись, успешный файл не удаляется и ошибка сохранения не показывается |
| ANS-021 | С включённой музыкой сохранить аудио в одном вопросе, перейти к следующему и сразу записать второе | обе записи сохраняются и воспроизводятся; позднее восстановление музыки не обрывает второй файл и сообщение «Запись не сохранилась» не появляется |
| ANS-022 | Дважды быстро нажать «Записать ещё», не меняя положение пальца | второй тап не попадает в появившуюся кнопку «Готово»; UI-stop недоступен первые 1,5 секунды, запись продолжается, файл короче 0,5 секунды не добавляется в список |
| ANS-023 | Открыть записи и начать запись на iPhone SE/Home Button, iPhone с Home Indicator и iPad | «Записать ещё» и «Готово» полностью видны, нижняя рамка не обрезана и между кнопкой и краем экрана остаётся отступ |
| ANS-024 | При открытой клавиатуре открыть уже существующие голосовые записи | клавиатура закрывается до показа шторки, список и нижняя кнопка полностью доступны |
| ANS-025 | С VoiceOver проверить короткую и длинную расшифровки | текст читается полностью; короткий текст не объявляется кнопкой; у длинного текста «Показать полностью»/«Свернуть» — отдельная фокусируемая кнопка |
| ANS-026 | Во время записи симулировать interruption/media-services reset, насколько позволяет устройство | ложный recording-overlay исчезает, аудиофокус освобождён и следующая запись запускается штатно |

### ИИ и спутник

| ID | Сценарий | Ожидаемый результат |
|---|---|---|
| AI-001 | AI-переменные отсутствуют | используются локальные вопросы, основной поток работает |
| AI-002 | Успешные ответы AI | вопросы не пустые, переходы не блокируются |
| AI-003 | Тайм-аут, offline, HTTP 4xx/5xx и невалидный JSON | есть безопасный fallback, нет вечной загрузки и unhandled rejection |
| AI-004 | Быстро завершить/сбросить сессию при незавершённом запросе | поздний ответ не меняет новую сессию |
| AI-005 | «Учитывать мои ответы» выключено | текст ответов отсутствует в AI-запросе |
| AI-006 | Настройка включена | UI явно предупреждает об отправке; передаётся только ожидаемый текст |
| AI-007 | Изменить настройку и перезапустить приложение | выбранное значение восстановлено из SQLite |
| AI-008 | Замедлить AI и проверить вход/несколько ротаций | готовый вопрос появляется без loader; pending-запрос дожидается своего результата без второго запроса и без преждевременного fallback; refill начинается после показа |
| AI-009 | Завершить молитву при быстром, медленном или недоступном AI | итоговый вопрос готовится за 15 секунд до нуля; готовый показывается сразу, pending показывает loader без промежуточного fallback; изменение ответа в последние 15 секунд обновляет prefetch; реальный fallback явно помечен как резервный |

### Писание

| ID | Сценарий | Ожидаемый результат |
|---|---|---|
| SCR-001 | Получить первый серверный отрывок и перейти дальше | первый запрос показывает loading, следующий отрывок берётся из prefetch и отличается |
| SCR-002 | Листать назад и снова вперёд | показывается фактический след без новых сетевых запросов и изменения исключений |
| SCR-003 | Добавить и убрать избранное | canonical snapshot сразу меняется и сохраняется после перезапуска |
| SCR-004 | Открыть длинный отрывок с nullable title | абзацы сохранены, отсутствие title не ломает читалку |
| SCR-005 | Выключить «Учитывать мои ответы» и пройти диалог | в сериализованном Scripture-request отсутствует `user_replies` |
| SCR-006 | Получить `safe_pool` или `retrieval_fallback` | отрывок отображается как обычный успех без технического сообщения |
| SCR-007 | 403, 422, 429, 503 и timeout | нет падения/бесконечного retry; технический `detail` не показывается |
| SCR-008 | Выбрать язык, перевод и озвучку, сохранить и перезапустить приложение | полная тройка восстановлена, новая сессия отправляет выбранные `language` и `translation` |
| SCR-009 | Сменить язык, затем перевод | дочерние списки очищаются; несовместимую или неполную тройку сохранить нельзя |
| SCR-010 | Открыть настройки без сети или при ошибке каталога | сохранённые подписи видны, есть повтор загрузки, ранее сохранённый выбор не повреждается |
| SCR-011 | Переключить язык/перевод после наличия offline-кэша | offline fallback не показывает снимок другого языка или перевода |
| SCR-012 | Чистая установка с поддерживаемым первичным языком устройства | выбирается серверный язык устройства и валидная translation/voice-тройка |
| SCR-013 | Чистая установка с неподдерживаемым языком устройства или недоступным каталогом | выбирается английский `en / 16 / 151` |
| SCR-014 | Изменить язык устройства после сохранения настройки | сохранённый пользовательский выбор не переопределяется |
| SCR-015 | Поставить озвучку Писания на паузу, нажать продолжить и сразу сменить режим или отрывок | старый отрывок не возобновляется после смены контекста; новый отрывок запускается штатно |
| SCR-008 | Запуск и навигация без сети | используется кэш показанных; при пустом кэше есть нейтральная ошибка и retry |
| SCR-009 | Ответ с canonical Psalm 23 и translated Psalm 22 | ссылка строится как «Псалом 22», из `passage` |
| SCR-010 | Ответ с `history_reset: true` | исключения сброшены, текущий ID добавлен заново, след и избранное сохранены |
| SCR-011 | Текст короче 160 символов переносится больше чем на три строки | карточка показывает «Читать целиком», reader открывает полный отрывок |
| SCR-012 | Максимально раскрыть reader с длинным отрывком на iPhone с Dynamic Island | верх reader остаётся ниже status bar; заголовок и кнопки не перекрываются |

### Рефлексия, завершение и серия дней

| ID | Сценарий | Ожидаемый результат |
|---|---|---|
| END-001 | Завершить без вывода | сессия завершается, пустая карточка вывода не показывается |
| END-002 | Завершить с выводом | текст показан на финальном экране и в дневнике |
| END-003 | Двойное нажатие на завершение | завершение и отметка дня выполняются один раз |
| END-004 | Две молитвы в один день | день засчитывается один раз, обе содержательные сессии есть в дневнике |
| END-005 | Завершение около полуночи и после смены timezone | серия и семь точек согласованы с локальной календарной датой |
| END-006 | Ввести вывод с открытой клавиатурой | экран переключается в компактный режим: вопрос и полная карточка ввода видимы над клавиатурой; после «Готово» видны действия завершения и возврата |

### Дневник и локальные данные

| ID | Сценарий | Ожидаемый результат |
|---|---|---|
| JRN-001 | Пустая история | понятное пустое состояние без ошибки |
| JRN-002 | Брошенная пустая сессия | не отображается в дневнике |
| JRN-003 | Поиск по цели, выводу, вопросу и ответу | находятся нужные записи |
| JRN-004 | Кириллический поиск с другим регистром | поиск остаётся регистронезависимым |
| JRN-005 | Открыть детали текстовой и голосовой молитвы | вопросы, ответы и записи привязаны правильно |
| JRN-006 | Воспроизвести аудио, закрыть детали, запустить другое | одновременно не играют два источника, UI плеера сбрасывается |
| JRN-007 | Удалить молитву | удаляются сессия, ответы, строки записей и файлы; день серии остаётся |
| JRN-008 | Перезапуск и установка новой сборки поверх старой | SQLite и аудиофайлы сохраняются и читаются |
| JRN-009 | Файл записи отсутствует, но строка БД осталась | экран не падает, проблема обрабатывается или понятно сообщается |
| JRN-010 | Открыть голосовой ответ без текста и нажать «Расшифровать» | появляется загрузка, затем текст под соответствующим аудиоплеером; после повторного открытия текст сохранён |
| JRN-011 | Искать по слову из расшифровки с другим регистром | находится нужная молитва |
| JRN-012 | Получить ошибку расшифровки в дневнике и повторить | аудио остаётся доступным, повторная попытка может сохранить текст |
| JRN-013 | Закрыть детали или удалить молитву во время расшифровки | запрос отменяется, поздний ответ не возвращает удалённые данные в UI или БД |

### Напоминания о молитве

| ID | Сценарий | Ожидаемый результат |
|---|---|---|
| REM-001 | Включить напоминания впервые | системный запрос разрешения появляется один раз, в момент включения, а не при старте приложения |
| REM-002 | Отказать в разрешении | тумблер остаётся выключенным, экран настроек работает, показан понятный текст про системные настройки |
| REM-003 | Выдать разрешение в системных настройках и вернуться в приложение | расписание планируется без перезапуска приложения |
| REM-004 | Задать ближайшее время и дождаться его | уведомление приходит с фразой из пула |
| REM-005 | Задать несколько времён и подмножество дней недели | строка расписания читается верно, уведомление приходит в каждое заданное время |
| REM-006 | Перезапустить приложение | расписание сохранено, фраза в следующем уведомлении может отличаться от прежней |
| REM-007 | Перезагрузить устройство | напоминание по расписанию всё равно приходит |
| REM-008 | Выключить тумблер напоминаний | все запланированные уведомления сняты, ничего не приходит |
| REM-009 | Тап по уведомлению с главной или из дневника | открывается главная |
| REM-010 | Тап по уведомлению во время идущей молитвы | сессия не прерывается, из молитвы не выбрасывает |
| REM-011 | Уведомление приходит при открытом приложении | показывается баннером, а не отбрасывается молча |
| REM-012 | Напоминание во время активного таймера молитвы | ongoing-хронометр (ADR-0010) не пропадает и не подменяется |
| REM-013 | Помолиться, дождаться времени напоминания в тот же день | напоминание приходит: согласовано напоминать всегда |

### Блокировка приложения

Защита опциональна и по умолчанию выключена; решение и границы —
[ADR-0014](../architect/decisions/0014-app-lock-pin-and-biometrics.md).

| ID | Сценарий | Ожидаемый результат |
|---|---|---|
| LOCK-001 | Защита не включалась | ни один экран не спрашивает код, в настройках нет строки смены кода |
| LOCK-002 | Включить пин из шести цифр и перезапустить приложение | ввод завершается только подтверждением пользователя, холодный старт открывается экраном блокировки, содержимое закрыто |
| LOCK-003 | Неверный, затем верный код на экране блокировки | неверный оставляет на экране с ошибкой, верный впускает без отдельного подтверждения |
| LOCK-004 | Сменить пин на код другой длины | смена требует текущий код, новый впускает, старый не подходит, экран ждёт новое число цифр |
| LOCK-005 | Выключить защиту | отмена и неверный код защиту не снимают, верный снимает, холодный старт больше не спрашивает код |
| LOCK-006 | «Забыли пин-код?» и два подтверждения | отмена на любом из двух шагов ничего не стирает, подтверждение стирает дневник и снимает защиту |
| LOCK-007 | Возврат из фона раньше минуты и позже минуты | короткое переключение код не спрашивает, больше минуты — спрашивает; процесс приложения не перезапускается |
| LOCK-008 | Снимок приложения в переключателе задач | вместо содержимого показана шторка приватности |
| LOCK-009 | Вход по Face ID / Touch ID | тумблер доступен только при включённом пине и зарегистрированном образце, отказ оставляет вход по коду |
| LOCK-010 | Образцы биометрии удалены в системе после включения тумблера | экран блокировки биометрию не предлагает, код продолжает работать |
| LOCK-011 | Пин-код и его хэш в логах и хранилище | пин нигде не хранится и не логируется, в Keychain только соль и хэш |

## 9. Нефункциональные проверки

### Интерфейс и доступность

- экраны 320–430 pt по ширине, iPhone с Dynamic Island и без него;
- iPad в поддерживаемой portrait-ориентации;
- клавиатура не закрывает поле и основные действия;
- длинные русские строки не обрезаются критически;
- кнопки имеют достаточную область нажатия и понятные accessibility labels;
- увеличенный системный текст, VoiceOver/TalkBack, Reduce Motion;
- контраст текста, состояний toggle и подтверждения удаления.

### Надёжность и производительность

- холодный старт без белого/зависшего экрана;
- отсутствие заметных зависаний анимации таймера и пламени;
- отсутствие роста памяти после 10 открытий шторок и воспроизведений;
- корректность после background/foreground, блокировки экрана и системного звонка;
- отсутствие незакрытых таймеров, записи микрофона и аудиоплеера после ухода с экрана.

### Безопасность и приватность

- Google/server master key отсутствует в клиентском бандле и логах;
- клиентский proxy key рассматривается как публичный и ограниченный;
- текст ответов отправляется только при включённой настройке;
- аудиофайлы хранятся в ожидаемой директории и отправляются в endpoint расшифровки только после явного нажатия;
- серверные логи и постоянное хранилище не содержат аудио, имени файла и расшифровки;
- логи не содержат молитвенные ответы, токены и полные сетевые payload;
- системное описание разрешения микрофона соответствует реальному использованию.

## 10. План автоматизации

### Этап A — базовые проверки

1. Оставить `typecheck` обязательным локальным и CI-гейтом.
2. Добавить Expo Doctor в воспроизводимую проверку окружения.
3. Добавить unit-тесты чистой логики: склонения, формат времени, календарные дни,
   выбор вопросов и разбор AI JSON.
4. Добавить интеграционные тесты хранилища и состояния с контролируемыми часами,
   моками SQLite, файлов и AI-клиента.

### Этап B — UI и E2E

1. Выбрать раннер после пробного сценария на реальном билде; первыми оценить
   Maestro и Detox.
2. Автоматизировать smoke без микрофона и внешнего AI.
3. Добавить стабильные testID/accessibility labels только там, где селекторы
   по пользовательскому тексту ненадёжны.
4. Аудио, системные permissions, background/foreground и визуальные дефекты
   оставить гибридными сценариями с ручной проверкой на физическом устройстве.

## 11. Классификация дефектов

| Severity | Значение |
|---|---|
| Critical | потеря/утечка данных, падение на основном сценарии, невозможность запустить приложение |
| Major | основной сценарий нельзя завершить без обходного пути; неверно сохраняются данные |
| Minor | функция работает с заметным дефектом или неудобством, есть простой обход |
| Trivial | косметическая проблема без влияния на выполнение сценария |

Приоритет исправления (`P0–P3`) назначается отдельно от severity с учётом частоты
и ценности сценария.

## 12. Критерии завершения первого прогона

Прогон завершён, когда:

- выполнены все `PRE`, `SMK` и функциональные сценарии первого iOS-окружения;
- для каждого сценария указан `Passed`, `Failed`, `Blocked` или `Not run`;
- у каждого `Failed` есть ClickUp Bug под соответствующим этапом и доказательство;
- нет открытых Critical-дефектов;
- Major-дефекты разобраны и имеют решение либо явное продуктовое принятие риска;
- зафиксированы ограничения покрытия Android, iPad, accessibility и background;
- итоговый отчёт содержит версии, commit, exit codes и полные логи проверок.
