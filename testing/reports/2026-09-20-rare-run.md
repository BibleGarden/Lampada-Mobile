# Rare-tier E2E run 2026-09-20

Group 3 (rare tier, 15 flows) plus the dependency suites carved out of it.
Two passes; final state: the tier is green except one documented NEEDS-ENV
flow.

## Pass 1: 4 passed / 11 failed

Dominated by one cascade: `lock-006-forgot-pin-prepare` enabled a PIN and the
planner ran its `wipe` pair before it (order is not guaranteed in tag runs);
six flows after that hit the lock screen (Keychain survives `clearState`).

## Fixes applied

- Interdependent flows untagged and chained into ordered suites:
  `ios-background-suite.yaml` (music → music-resume → timer-setup →
  timer-resume) and `ios-lock-006-suite.yaml` (prepare → wipe). Both suites
  pass end-to-end (exit 0).
- `stage03-long-goal`: threshold kicker is «ПЕРЕД НАЧАЛОМ» now.
- `scr-003-long-reader`: audio assert uses `scripture-audio-button` (Maestro
  matches accessibility labels full-string).
- `scripture-translation-labels`: the default quote language now follows the
  device locale, so the flow explicitly switches the picker to English first;
  the picker sheet auto-advances (no separate translation-picker tap).
- `lock-007-background-timeout`: the home-screen icon is «Лампада», not
  «Twinkler»; passed solo after the fix (cold-start PIN settle wait was also
  added to lock-002, see the main run report).
- `update-soft` / `update-hard` / `scripture-legacy-favorites`: NEEDS-ENV,
  untagged; moved to the env phase (task 86cbj95j4). The update banner is
  fully server-driven (`lib/versionCheck.ts`); there is no debug hook, so the
  flows need a stubbed `/api/version-check`.

## Final state

- Tag run: 7 passed / 1 failed — the only failure is
  `jrn-008-launch-after-upgrade`, which needs a manually seeded journal entry
  (NEEDS-ENV, task 86cbj95j4).
- background-suite: exit 0. lock-006-suite: exit 0.

Logs: /tmp/e2e-rare-run.log, /tmp/e2e-rare-run2.log,
/tmp/e2e-batch1/background-suite.log, /tmp/e2e-batch1/lock006-suite.log
(kept in /tmp per evidence policy).
